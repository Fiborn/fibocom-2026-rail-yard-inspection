# Contributing

本仓库按 FiBoom-project 模板整理，用于项目展示、代码审阅和二次开发。

## 提交建议

1. 不提交真实 API Key、访问令牌、账号密码或本机专属配置。
2. 业务算法、模型和边缘计算说明放入 `edge_computing/`。
3. PCB、结构件和 BOM 等硬件资料放入 `hardware/`。
4. STM32H750、ESP32/L610 等设备固件放入 `firmware/`。
5. 云端服务、Web 前端和移动端内容放入 `cloud/`。
6. 启动、停止、辅助调试脚本放入 `tools/`。
7. 项目报告、部署说明、使用说明放入 `docs/project/`。

## 本地配置

如需配置 DeepSeek 或高德地图等服务，请复制 `.env.example` 并在本地填写，不要将真实密钥提交到仓库。
