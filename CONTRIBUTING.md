# Contributing

本仓库按 FiBoom-project 模板整理，用于项目展示、代码审阅和二次开发。

## 提交建议

1. 不提交真实 API Key、访问令牌、账号密码或本机专属配置。
2. 后端、前端和边缘识别相关内容放入 `edge_computing/`。
3. ESP32、舵机库、接线或嵌入式控制相关内容放入 `hardware/`。
4. 可烧录固件放入 `firmware/`。
5. 启动、停止、辅助调试脚本放入 `tools/`。
6. 项目报告、部署说明、使用说明放入 `docs/`。

## 本地配置

如需配置 DeepSeek 或高德地图等服务，请复制 `.env.example` 并在本地填写，不要将真实密钥提交到仓库。
